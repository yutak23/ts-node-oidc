declare const array: string[];

const hello = (name: string): string => {
	debugger;
	const aaa = '';
	for (const i in array) {
		console.log(array[i]);
	}
	return `Hello, ${name}!`;
};
export default hello;
